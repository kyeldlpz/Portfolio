/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true, // Disable optimization for better compatibility with SVGs and static assets
  },
}

module.exports = nextConfig

